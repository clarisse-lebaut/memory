// --- COMPONENT : RANDOMBUTTON TO RELOAD THE PAGE

import React from "react";

const RandomButton = ({ onClick }) => {
  return <button onClick={onClick}>COMMENCER LA PARTIE</button>;
};

export default RandomButton;
